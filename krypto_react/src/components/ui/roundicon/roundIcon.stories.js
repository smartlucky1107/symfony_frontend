import React from "react";

import RoundIcon from "./roundIcon";

export default {
  title: "ui/roundIcon",
  component: RoundIcon,
};

const Template = (args) => <RoundIcon {...args}></RoundIcon>;

export const roundIcon = Template.bind({});
